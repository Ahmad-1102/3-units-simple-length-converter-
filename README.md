# 📏 Length Converter - JavaScript

A simple web-based length converter that converts between miles, kilometers, and feet.

Features

- Convert between 3 length units:
  - Miles (mi)
  - Kilometers (km)  
  - Feet (ft)
- Real-time conversion
- Error handling for invalid units
- Clean console output

Quick Start

1. Clone the repository or download `7i.html`
2. Open `7i.html` in your browser
3. Open browser console (F12 → Console tab)
4. Click the "click" button

## 💻 Usage Example

```javascript
// Basic usage
convertLength(72200, 'miles', 'km');  // Converts 72200 miles to kilometers
convertLength(100, 'km', 'miles');    // Converts 100 km to miles
convertLength(10, 'miles', 'ft');     // Converts 10 miles to feet
